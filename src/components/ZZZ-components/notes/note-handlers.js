// export const creatorHandler = (e, note, setNote) => {
//     e.target.value
//         ? setNote({
//             ...note,
//             creator: e.target.value,
//         })
//         : setNote({ ...note, creator: null });
// };

// export const titleHandler = (e, note, setNote) => {
//     e.target.value
//         ? setNote({
//             ...note,
//             title: e.target.value,
//         })
//         : setNote({ ...note, title: null });
// };

// export const subtitleHandler = (e, note, setNote) => {
//     e.target.value
//         ? setNote({
//             ...note,
//             subtitle: e.target.value,
//         })
//         : setNote({ ...note, subtitle: null });
// };

// export const topicHandler = (e, note, setNote) => {
//     e.target.value
//         ? setNote({
//             ...note,
//             topic: e.target.value,
//         })
//         : setNote({ ...note, topic: null });
// };

// export const imageURLHandler = (e, note, setNote) => {
//     e.target.value
//         ? setNote({
//             ...note,
//             imageURL: e.target.value,
//         })
//         : setNote({ ...note, imageURL: null });
// };

// export const textHandler = (e, note, setNote) => {
//     e.target.value
//         ? setNote({
//             ...note,
//             text: e.target.value,
//         })
//         : setNote({ ...note, text: null });
// };

// export const tagsHandler = (e, note, setNote) => {
//     e.target.value
//         ? setNote({
//             ...note,
//             tags: e.target.value.split(",").filter(Boolean),
//         })
//         : setNote({ ...note, tags: null });
// };